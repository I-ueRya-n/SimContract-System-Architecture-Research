"""Per-role policy packages for the energy-market domain.

Each role owns its deterministic default policy (``defaults.py``) and its
persona archetypes (``archetypes.py``). Reactive NPC policies (``npc.py``) are
added per role when that capability is implemented.
"""
